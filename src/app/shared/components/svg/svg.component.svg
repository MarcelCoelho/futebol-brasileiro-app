<svg>
  <g>
    <rect x="30" y="12" width="12px" height="12px" [attr.fill]="fillColor" (click)="changeColor()" />
    <!-- <text x="120" y="50">click the rectangle to change the fill color</text> -->
  </g>
</svg>